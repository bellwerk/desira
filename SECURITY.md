# Security

The active Desira security model lives in [`docs/SECURITY.md`](docs/SECURITY.md).

This root file exists only as a pointer so the repository does not keep a stale
template security policy beside the real project-specific document.
